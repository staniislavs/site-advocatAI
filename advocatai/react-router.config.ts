import type { Config } from "@react-router/dev/config";

export default {
  // Config options...
  // Server-side rendering enabled by default
  ssr: true,
  buildDirectory: "dist",
} satisfies Config;
